<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./header.ejs') %>
        <title> page </title>
    </head>
    <body>
        <%- include('./navbar.ejs') %>
        <h4 class="text-center"> Voici l'ami(e) choisi(e) :</h4>
        <div class="d-flex justify-content-center">
            <div class="card" style="width: 35rem;">
                <img src='/public/img/profil<%= user.data.id %>.jpg' class="card-img-top" alt="img">
                <div class="card-body text-center">
                  <h5 class="card-title"><%= user.data.name %></h5>
                  <p class="card-text"><%= user.data.email %></p>
                  <p class="card-text"><%= user.data.phone %></p>
                  <p class="card-text"><%= user.data.address %></p>
                  <a href="#" class="btn btn-warning">Modifier</a>
                </div>
            </div>
        </div>
    </body>
</html>